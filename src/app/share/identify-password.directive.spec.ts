import { IdentifyPasswordDirective } from './identify-password.directive';

describe('IdentifyPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentifyPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
