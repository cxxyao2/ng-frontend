import { CreatePasswordStrengthDirective } from './create-password-strength.directive';

describe('CreatePasswordStrengthDirective', () => {
  it('should create an instance', () => {
    const directive = new CreatePasswordStrengthDirective();
    expect(directive).toBeTruthy();
  });
});
